#[derive(Debug, Default)]
pub struct Map;

impl Map {
    pub fn calculate_navgraph(&self) -> NavGraph {
        // TODO:
        NavGraph::default()
    }
}

#[derive(Debug, Default)]
pub struct NavGraph;
